import 'package:flutter/material.dart';

class DiscoveyTab extends StatelessWidget {
  const DiscoveyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Discovery Tab'),
      ),
    );
  }
}
